import React from "react";

import Minimoground from "../../components/newCategories/minimoground";

export default {
  title: "newCategory/Minimoground",
  component: Minimoground,
};

const Template = () => <Minimoground />;
export const Minimogcategories = Template.bind({});
